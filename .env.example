# Public portfolio origin used when the local manager copies shareable links.
VITE_PUBLIC_PORTFOLIO_URL=https://dbp-jack.github.io/Profile/

# Never place tokens, passwords, signed URLs, or private content in VITE_* values.
