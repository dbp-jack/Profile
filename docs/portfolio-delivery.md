# 최신 정본과 기업별 지원 작업 인계

확인일: 2026-09-10. 새 작업은 이 문서와 루트 `AGENTS.md`, `README.md`에서 시작합니다. 긴 과거 대화나 이전 시안의 구성을 현재 지시로 복원하지 않습니다.

## 현재 상태와 정본

| 대상 | 현재 기준 |
| --- | --- |
| 편집 저장소 | `/Users/minsujeong/Desktop/재적3/Portfolio` — 하나의 canonical workspace |
| 공개 웹 | https://dbp-jack.github.io/Profile/ — `src/pages/home/` |
| PDF 페이지 | https://dbp-jack.github.io/Profile/pdf/ — `src/pages/pdf/`의 26쪽 |
| 관리 도구 | http://127.0.0.1:5174/manage — `src/pages/manage/`, 로컬 전용 |
| 웹·PDF 본문 확정 배포 | 커밋 `8557b36e2bb598fe38e1ea3b71ea6ea05caf55c9`, [Actions 34368037726](https://github.com/dbp-jack/Profile/actions/runs/34368037726) |
| 이력서 | 아래 검증된 `v5_3` 3쪽 PDF 하나. 과거 상세·함축본은 정본에서 제외 |

위 배포는 PDF 1쪽 웹 링크와 네 단계 관리 도구가 확정된 기준입니다. **이전 시안 경로·문서 정리와 v5_3 이력서 규칙**은 같은 `main` 브랜치의 후속 변경으로 관리합니다. 최신 커밋은 `git log -1`, 실제 배포 성공은 [GitHub Actions](https://github.com/dbp-jack/Profile/actions/workflows/deploy.yml)에서 해당 커밋의 실행 결과로 확인합니다. 과거의 ‘로컬 작업 중’ 기록만으로 미완료라고 판단하지 않습니다. 새 작업은 `git status --short --branch`와 `git worktree list --porcelain`을 확인하고, 진행 중인 다른 변경이 있으면 보존합니다.

## 사용자가 확정한 구성

- 웹은 소개·기술 → FeedShop 소개·문제 해결 2개·회고 → 3M 소개·문제 해결 1개·회고 → 협업·AI → 경험 → 마무리·연락처·자료를 한 문서에서 읽습니다.
- 3M의 Auth·User 경계 분리와 인증 정보 전달은 하나의 문제 해결 안에서 설명하는 적용 단계입니다. 개수를 맞추려고 별도 사례로 나누지 않습니다.
- 핵심 문제·대안·구조·검증·회고는 바로 보입니다. 보조 측정 자료만 펼쳐봅니다. 기술 목록과 하단 자료 링크에는 토글을 다시 넣지 않습니다.
- 소개의 링크는 LinkedIn·GitHub이며, PDF 보기는 상단 오른쪽에 있습니다. SQL 42회·2회·캐시 적중 0회 이미지는 가로 3열과 원본 비율을 유지하고 같은 페이지에서 확대합니다.
- 웹 마무리의 `근거를 확인하고, 팀이 이해할 수 있게 설명합니다`와 PDF 표지의 `수치로 검증하고, 팀 흐름을 움직이는 백엔드 개발자`는 각각 강제 줄바꿈 없는 한 문단입니다.
- PDF는 26쪽입니다. 문제·배경 → 대안 비교·구현 → 검증 → 최종 결과 순서이며 결과부터 시작하는 RAC 배치로 되돌리지 않습니다.
- PDF 4·15쪽은 소개·기간 → 가로 서비스 흐름 → 담당/효과 두 열 → 기술의 배치를 유지합니다. 증거 이미지·대안 비교·회고·협업 자료·AI 현재/다음 단계·목차·마무리를 임의로 빼지 않습니다.
- 누구나 이해할 쉬운 단어를 우선합니다. 제목이나 짧은 항목에 불필요한 마침표를 붙이지 않습니다. 완결된 문장은 마침표를 쓸 수 있습니다.
- 핵심 수치와 테두리는 파란색, 연락처는 링크와 구분되는 청록색입니다. 회고는 성과 목록이나 다음 할 일이 아니라 **경험을 통해 얻은 배움과 판단 기준**을 설명합니다.
- 기업별 우선순위는 달라질 수 있지만 기본 프로젝트는 FeedShop·3M 두 개입니다. FIX는 기존 관리 선택지이며 필요할 때 별도 검토합니다. FlexiRoute는 현재 노출 대상이 아닙니다.

세부 배치는 [웹 README](../src/pages/home/README.md), [PDF README](../src/pages/pdf/README.md), [관리 README](../src/pages/manage/README.md)를 따릅니다. 멘토 조언의 출처와 통합 기준은 [피드백 기준](mentor-feedback-rubric.md)에 보존합니다.

## 성과를 설명할 때 지킬 근거

| 항목 | 확인된 범위와 주의점 |
| --- | --- |
| FeedShop 목록 조회 | 동시 1,000명 평균 응답시간 `6.818 → 4.191 → 0.638초`. 조회 구조 개선과 Redis를 단계별 적용한 값 |
| 위 조회 테스트 오류 | Redis 단계 원본은 **49,931건 중 오류 732건(약 1.47%)**. 오류 없는 성능 검증으로 표현하거나 오류 수를 감추지 않음 |
| DB 조회 수 | 요청당 42회 → 2회, 캐시 적중 요청 0회. Scouter 개별 요청 관찰값으로, 부하 테스트 평균 응답시간과 다른 지표 |
| 투표 검증 | 500·1,000·3,000명 테스트 HTTP 오류 0, 평균 약 0.83·2.19·5.00초. 목록 조회와 다른 요청이며 DB 중복 저장·DB/Redis 집계 대조는 별도 확인 |
| 3M의 0건 | User 모듈 소스에서 Auth 패키지를 import하는 수가 0. 전체 장애·모든 의존성이 0이라는 의미가 아님 |
| 권한 검증 | 로컬 H2에서 Gateway·Auth·User를 연결한 허용·권한 거부·미인증 응답 검증. 운영 전체 검증으로 확대하지 않음 |
| 재방문·이탈 방지 | 서비스 목적과 개선 의도. 실제 이탈률·매출·재방문율을 측정한 성과로 주장하지 않음 |
| 조사 46.9% | 2022년 도·소매업 소상공인의 경쟁 심화 응답 비율. 소상공인 전체 46.6%와 구분 |
| AI 플래너 | 사용자 제공 화면과 설명 기반. 챌린지 참여 중 등 시점에 따라 바뀌는 상태는 지원 시 다시 확인 |

근거 파일은 `src/content/projects/feedshop.ts`, `threeM.ts`, `src/pages/pdf/content.tsx`, `public`의 원본 이미지와 프로젝트 GitHub/Wiki입니다. 채용공고에 맞추려는 목적으로 실제 경험·운영 성과·수치를 추가하지 않습니다. 다른 부하 조건의 성과를 쓰려면 원본을 다시 확인하고 같은 조건끼리 비교합니다. 이번 정리는 새로운 부하 테스트나 오류 해결을 수행한 것이 아닙니다.

## 이력서와 멘토 원문

- 이력서: `/Users/minsujeong/Downloads/01_커리어_지원/자소서_면접/00_Current_Resume/멘토리용 이력서_v5_3.pdf`
- 같은 폴더 `README.md`의 등록 기록과 대조해 2026-09-10에 3쪽, SHA-256 `2a92c0e3517f9cf001176368e3fb35646191555dfa9c8d105fb120e6f4b0fa85`가 일치함을 확인했습니다. 외부 README 본문의 경로는 이동 전 위치이며, 실제 존재하는 위 경로를 사용합니다.
- 이번 인계에서 이력서 전체 내용을 새로 검토한 것은 아닙니다. 기업별 문구를 제안하기 전에 원본 PDF와 README를 직접 읽습니다. 이름이 비슷한 다른 v5_3 파일이나 v5_2 상세·함축본을 대신 선택하지 않습니다.
- 김현수 1차: `/Users/minsujeong/Downloads/01_커리어_지원/멘토링_특강/멘토링_피드백_김현수_1차.pdf`
- 김현수 2차: `/Users/minsujeong/Downloads/01_커리어_지원/멘토링_특강/멘토링_피드백_김현수_2차.pdf`
- 페이지 수정 전 두 원문의 관련 조언을 확인합니다. 문구 검토와 실제 파일 수정은 구분하고, 이력서 원본은 명시적 요청 없이 덮어쓰지 않습니다. 공통 면접 자료는 기업별 답변과 분리합니다.

## 새 기업별 작업의 진행 순서

1. **기업명과 지원공고 URL 또는 전문**을 받습니다. 이번 인계 시점에는 대상 기업이 정해지지 않았습니다. 과거 뱅카우 지원 기록을 현재 대상으로 추정하지 않습니다.
2. 공식 공고의 업무·필수 요건·우대 사항·제출 형식을 확인합니다. 변동 가능한 정보는 확인 날짜와 출처를 남기고, 공고의 사실과 지원자의 해석을 구분합니다.
3. `공고 요구사항 / 본인 근거 / 강조할 경험 / 부족하거나 확인할 점`을 짧은 표로 제시합니다. 프로젝트 순서, 소개 문구와 마무리에서 무엇을 바꿀지 쉬운 말로 설명합니다.
4. `/manage`의 **기본값·현재 선택·이미 제공되는 대안**을 먼저 대조합니다. 회사별 문구는 해당 기업의 구성과 URL로 분리하며, 공통 기본값이나 기존 지원 기업의 구성을 덮어쓰지 않습니다. 구현·문구 반영은 사용자 승인 범위 안에서 진행합니다.
5. 승인한 선택은 미리보기·생성 URL·새로고침 복원·기본값 복귀로 확인합니다. 저장 상태는 브라우저와 origin별로 다르므로 `127.0.0.1:5174`를 기준으로 사용합니다.
6. **관리 URL은 웹에만 적용됩니다.** PDF는 공통 26쪽입니다. 기업별 이력서/PDF가 필요하면 형식·저장 위치·원본 보존을 정한 뒤 별도로 만들고 내용·레이아웃을 검증합니다. 링크 생성만으로 PDF까지 맞춤화됐다고 말하지 않습니다.
7. 최종 전달은 해당 기업의 강조 근거, 수정 문안, 공유 URL 또는 승인한 제출 파일로 마칩니다. 지원서 제출이나 이메일 발송은 별도 명시적 요청이 있어야 합니다.

회사마다 새 작업을 사용할 때도 이 문서에서 시작합니다. 첫 응답은 준비 상태를 짧게 알리고 기업명과 공고를 요청하면 됩니다. 전체 포트폴리오 재설계, 기본값 초기화, 서버 재시작, 배포부터 자동 실행하지 않습니다.

## 로컬 관리와 저장

- 네 단계: **프로젝트·구성 → 소개 문구 → 지원 기업 → 검토·공유**. 프로젝트는 두 칸에서 교체·순서 변경하고 기업 이름으로 맞춤 문구를 시작합니다.
- 현재 선택은 자동 저장합니다. 이름 있는 구성 저장/불러오기, 직전 변경 취소, 기본값 복귀를 제공합니다.
- 저장 키 `portfolio-manager-workspace-v2`에 현재 선택(`draft`), 저장 구성(`saved`), 기업별 문구(`companies`)를 둡니다. 새 키가 없거나 손상되면 기존 키를 읽으며, 과거 저장 자료를 일괄 삭제하지 않습니다.
- 저장 구성에는 강점 선택도 포함됩니다. 기본값 복귀는 현재 선택만 되돌리고 저장한 구성·기업 문구는 보존합니다. 다른 브라우저나 `localhost`와 `127.0.0.1` 사이에는 자동 동기화되지 않습니다.
- 공유 URL에는 공개 문구가 포함됩니다. 현재 선택에 맞는 **전체 생성 URL**을 사용해야 다른 브라우저에서도 재현됩니다. 긴 링크는 제출 시스템에서 잘리는지 확인합니다.
- 기존 기능으로 기업별 링크를 만들 때마다 커밋·push할 필요는 없습니다. 디자인·공개 콘텐츠·URL 계약을 수정할 때만 별도 배포가 필요합니다.
- `/manage`는 로컬 전용이며 production 빌드에 포함되지 않습니다. 미리보기는 같은 공개 홈을 렌더링합니다.

## 공개 포트폴리오

- GitHub Pages는 기본 포트폴리오를 `/`에서 제공합니다.
- `?preset=project-focused`는 미리 정의한 공개 구성을 선택합니다.
- `?blocks=hero,about,projects,resources,contact,footer`는 공개할 페이지 블록을 선택합니다.
- `?projects=feedshop,three-m`은 프로젝트 블록을 선택하고 순서를 정합니다.
- `?copy=backend-impact`는 준비된 문구 프로필을 선택합니다.
- `?strengths=backend-validation`은 독립된 Strengths 구성을 선택합니다. 기본 구성은 쿼리에 표시하지 않습니다.
- `?company=ably`는 기업 식별자를 부여합니다. 관리 화면에서 생성한 전체 공유 URL은 `blocks`, `projects`, `copy`, `company`, 선택적 `strengths` 값을 함께 담아 다른 브라우저에서도 같은 공개 구성을 재현합니다.
- `?ci=1`이 포함된 링크는 기업명, 서비스 방향, 기업 이해, 내 경험·기여, 키워드, 서비스 흐름, 선택적 로고 URL을 같은 쿼리에 담아 `Company Direction` 카드를 재현합니다.
- 기본 URL은 공통 포트폴리오만 표시하며 `Company Direction` 기업 맞춤 카드를 노출하지 않습니다.
- `?company=test`와 뱅카우 회사 키(`bankcow`, `bancow`, `stockkeeper`, `stock-keeper`)는 미리 등록된 뱅카우 `Company Direction` 카드를 표시합니다. 관리 화면에서 맞춤 내용을 활성화하면 같은 키의 미리 등록된 카드보다 URL의 맞춤 내용을 우선합니다.
- 쿼리 파라미터는 표시 방식만 제어합니다. 접근 제어 수단이 아니며 비공개 자료를 노출하는 데 사용하면 안 됩니다.

## 배포와 과거 자료 정리

- GitHub Pages는 `main` push 시 `.github/workflows/deploy.yml`에서 Node 20, `npm ci`, `npm run build`, `BASE_PATH=/Profile/`로 배포합니다. 로컬 수정 완료와 Actions 배포 성공을 구분합니다.
- `/web-preview` → `/`, `/pdf-compare` → `/pdf`로 로컬 이전 주소를 연결합니다. 쿼리와 해시를 보존하고 별도 시안 컴포넌트·미리보기 전용 PDF 모드는 제거했습니다.
- 이전 32쪽 `output/pdf/portfolio-latest.pdf`, `portfolio-master.pdf`와 7월 검증 화면은 `output/archive/2026-09-10-obsolete-portfolio/`에 복구 가능하게 옮겼습니다. 이름에 latest가 있어도 현재본이 아닙니다.
- 현재 제출용으로 별도 생성·확인한 PDF 파일은 이 인계에 등록되어 있지 않습니다. 현재 `/pdf`의 저장 기능으로 생성한 결과를 확인해야 합니다.
- 오래된 페이지 검토 기록은 Git `8557b36`의 해당 README·rubric에서 확인할 수 있습니다. 현재 기본값으로 복원하는 자료가 아닙니다.
- 프로젝트 증거 이미지, 관리 저장 자료, 면접 기록, `output/wiki`의 보호 자료와 로컬 도구는 보존합니다. 새 정리는 관련 없는 파일을 일괄 삭제하는 권한이 아닙니다.

## 이번 인계 정리의 검증

- 2026-09-10: 관련 구성·관리 테스트 13개, TypeScript 포함 production build, 수정 TSX ESLint, Git 공백 검사 통과
- canonical 5174의 실제 cwd 확인. 이전 PDF 주소의 15쪽 → 정식 `/pdf?page=15`, 26쪽 표시·저장 버튼 확인. 이전 웹 주소의 프로젝트 순서·소개 선택·연락처 해시 보존 확인. 기존 `/manage`의 네 단계 화면 확인
- PDF 문구·배치, 웹 배치와 관리 기능 소스는 이번 정리에서 바꾸지 않았습니다. 전체 사이트 회귀·PDF 재출력은 수행하지 않았습니다. 후속 커밋·배포 상태는 위 Actions에서 확인합니다.
- 격리한 PDF의 원본 해시와 이력서 해시 일치, 보호 자료 유지, 관련 없는 non-ignored 미추적 파일 0개, worktree 1개 확인

## 비공개 근거 자료

- 비공개 근거, 자격 증명, 접근 토큰, 서명 URL을 `src/content`, `public`, `VITE_*` 환경 변수에 넣지 않습니다.
- `src/portfolio-builder/private-content.ts`의 `PrivateContentProvider`는 향후 인증 서버를 연결하기 위한 인터페이스이며, 현재 실제 인증 연동은 없습니다.
- 향후 서버는 사용자를 인증하고 각 근거 자료 요청을 인가한 뒤 짧은 수명의 데이터만 반환해야 합니다.
- 세션은 HttpOnly 보안 쿠키를 우선하며, 공개 GitHub Pages는 비공개 API가 없어도 정상적으로 사용할 수 있어야 합니다.
